#include <stdio.h>
#define TRUE 1
#define FALSE 0
int main(){
    printf(" if the number is True then it will print: %d",TRUE);
    printf("\nif the number is False then it will print: %d \n",FALSE);

return 0;
}